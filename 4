#include <stdio.h>

int main() {
    double faturamento_SP = 67836.43;
    double faturamento_RJ = 36678.66;
    double faturamento_MG = 29229.88;
    double faturamento_ES = 27165.48;
    double faturamento_Outros = 19849.53;

    double total = faturamento_SP + faturamento_RJ + faturamento_MG + faturamento_ES + faturamento_Outros;

    printf("SP: %.2f%%\n", (faturamento_SP / total) * 100);
    printf("RJ: %.2f%%\n", (faturamento_RJ / total) * 100);
    printf("MG: %.2f%%\n", (faturamento_MG / total) * 100);
    printf("ES: %.2f%%\n", (faturamento_ES / total) * 100);
    printf("Outros: %.2f%%\n", (faturamento_Outros / total) * 100);

    return 0;
}